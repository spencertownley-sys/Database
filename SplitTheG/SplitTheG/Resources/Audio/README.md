# Audio assets

The MVP synthesizes all audio in code (`AudioSynth` in
`Services/AudioService.swift`): the looping flow bed, the set-down thump, the
cascade wash, and the grade stingers. Drop recorded replacements in here and
swap the loaders in `AudioService` when real sound design lands.

No file in this folder may reference a real beverage brand.
